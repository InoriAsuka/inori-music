# Phase v3.5.0 — 测试覆盖 + CI 完善

**版本**: v3.5.0  
**状态**: done  
**方向**: 单元测试 / Widget 测试 / Integration 测试 + CI 三端构建

## 背景

v3.0.x 的 `flutter test` 仅有 8 个纯 Dart 测试（AuthState）；PlayerNotifier、FavoritesNotifier、HistoryNotifier、所有 Widget 均无覆盖，CI 也未验证 iOS 构建。

## 目标

- 单元测试 ≥ 30 cases
- Widget 测试 ≥ 10 cases  
- CI：APK + IPA（无签名）+ macOS 三端全绿

## 任务清单

### 单元测试

- [x] `test/player_notifier_test.dart`
  - PlayerState 状态机（defaults / copyWith / isIdle / queue reorder / RepeatMode）
  - 13 cases（无需 mock AudioPlayer，测纯状态逻辑）
- [x] `test/track_favorite_notifier_test.dart`
  - 本地镜像 Notifier（避开破损 inori_api generated 导入链）
  - 测试：init 幂等、toggle on/off、optimistic rollback、family 独立性
  - 6 cases
- [x] `test/history_notifier_test.dart`
  - 本地 stub provider（避开破损 inori_api generated 导入链）
  - 测试：empty / seeded / loading / error / batch-delete 语义
  - 6 cases
- [x] `test/artist_cache_notifier_test.dart`（v3.0.2 引入）
  - ≥ 3 cases
- [x] `test/artwork_url_provider_test.dart`（v3.1.0 引入）
  - Stub Dio interceptor 测试 200/404/network-error/empty-albumId
  - 4 cases
- [x] `test/download_manager_test.dart`（v3.4.0 引入）
  - 5 cases (pure state-machine, no DB required)

### Widget 测试

- [x] `test/mini_player_bar_test.dart`
  - 播放状态下显示正确 title/artist；pause 按钮切换
  - ≥ 3 cases
- [x] `test/track_list_tile_test.dart`
  - 显示 title；artistId 回退；空 artistId 无 subtitle
  - ≥ 3 cases (3rd case added)
- [x] `test/login_screen_test.dart`
  - AuthState（defaults / authenticated / copyWith / error clear）+ LoginScreen smoke
  - 5 cases

### CI（`.github/workflows/mobile.yml`）

- [x] 新增 `build-ios` job（`runs-on: macos-15`）
  - `flutter build ipa --no-codesign`
  - artifact 上传 `build/ios/archive/`
- [x] `test` job：`flutter test --coverage` + coverage report artifact
- [x] 三端 job 并行（depends on `analyze` + `test` jobs）
- [ ] `build-macos` job 迁移至 `runs-on: macos-15`（保留 macos-latest 以确保兼容）— 不影响测试覆盖目标，留后续 CI 优化

### Integration 测试（可选，Maestro）

- [ ] 安装 `maestro` CLI（未实现 — Maestro 需要真机/模拟器，CI 环境复杂）
- [ ] `e2e/login_and_play.yaml`（未实现）
- [ ] CI 中 Android Emulator job（未实现）

## 非目标

- 100% 覆盖率
- iOS 真机签名
- Windows CI（GitHub macOS/ubuntu runner 已覆盖 android + ios + macos）

## 后续候选

- 截图测试（golden tests）
- 性能测试（`flutter drive --profile`）
