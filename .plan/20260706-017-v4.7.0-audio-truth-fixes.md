# Phase v4.7.0 — 播放器音频真实性修复

**版本**: v4.7.0
**状态**: complete
**方向**: Flutter 客户端（音频引擎核心缺陷修复）

## 背景

2026-07-06 全量代码审查（逐行核对 v4.1.0–v4.2.0 声称完成的音频特性）发现多项特性为
**假实现或存在严重缺陷**，requirement.md 记录与实际代码不符：

1. **EQ 均衡器无音频效果**：requirement v4.1.1 声称"引入 `just_audio_equalizer` 插件"，但
   `pubspec.yaml` 从未加入该依赖（仅有 `just_audio: ^0.9.44` / `just_audio_background` /
   `audio_service`）；`audio_handler.dart:103` 的 `applyEqualizerBands` 经
   `(_player as dynamic).setBands(bands)` 调用一个 `AudioPlayer` 上**不存在的方法**，
   NoSuchMethodError 被空 catch 吞掉。Settings 页的 10 条频段滑块（`settings_screen.dart`
   `_EqSection`）是纯 UI，对音频输出零影响。
2. **ReplayGain 从未被应用**：`replay_gain_notifier.dart:11` 注释声称"实际增益应用发生在
   PlayerNotifier.playTrack"，但 `player_notifier.dart` 全文没有任何代码读取
   `replayGainEnabledProvider` 或 track 的 `replayGainDb` 字段。v4.5.0 服务端 ffmpeg 自动
   分析出的响度值在客户端无消费方。
3. **Shuffle 是空操作**：`player_notifier.dart:291` `setShuffle` 只翻转 `state.shuffle`
   标志位；`next()`/`previous()` 始终按顺序索引推进，从不参考该标志。
4. **Crossfade 不是交叉淡化**：`audio_handler.dart:137` `_runCrossfade` 在
   `currentIndexStream` 变化**之后**对同一个 player 做音量 1→0→1 的 V 形凹陷——切歌后新曲目
   先被压低再抬回，不是两轨重叠淡化；与 requirement v4.2.0 声称的"clip model 实现"不符。
   它还会覆写用户音量、暂停时可能停在半音量。
5. **Gapless 队列播放状态脱轨（最严重）**：`PlayerNotifier` 不监听
   `_audioPlayer.currentIndexStream`。`ConcatenatingAudioSource` 内部自动切到下一曲时，
   `state.currentIndex`/`state.mediaItem`/通知栏元数据全部停留在旧曲目，`_postHistory`
   不触发（`processingStateStream.completed` 只在**整个队列**播完时触发一次）；队列播完后
   `next()` 的 `% queue.length` 取模使队列无限循环，无视 `RepeatMode.off`。
6. **切歌性能**：`_playAtIndex` → `playTrack` → `_buildConcatQueue` 每次手动切歌都串行
   重解析全队列 URL（N 个网络请求）并整体重建音源，大队列下明显卡顿，也破坏了 gapless 缓冲。
7. **v4.1.1 声称的"presigned URL 提前 120s 刷新（currentIndexStream 监听）"在代码中不存在**
   （全库仅 `initCrossfade` 一处使用 `currentIndexStream`）。

注意：v4.6.0 的「EQ 自定义预设保存与命名」只涉及设置数据持久化，与本阶段无冲突；
但只有本阶段完成 EQ 真实接线后，那些预设才真正可听。建议 v4.6.0 → v4.7.0 顺序执行
（两者都改 `player_notifier.dart`，避免并行冲突）。

## 目标

- 队列 gapless 自动前进时 UI/通知栏/历史上报与实际播放曲目始终一致
- RepeatMode.off 播完停止、all 循环、one 单曲循环，语义正确
- ReplayGain 开关打开时实际调整播放增益
- Shuffle 开关实际改变播放顺序
- EQ 在 Android 上真实作用于音频输出；不支持的平台明确标注禁用而非静默无效
- Crossfade 设置项行为与其名称/文档一致
- 手动切歌不再全队列重建，即点即切

## 任务清单

### Gapless 状态同步（`lib/src/player/player_notifier.dart`）

- [x] `_setupPlayerListeners` 新增 `_audioPlayer.currentIndexStream` 订阅：concat 源索引变化时
      更新 `state.currentIndex`、解析并更新 `state.mediaItem`、推送 `audioHandler.mediaItem`
- [x] 历史上报改为在 index 前进时对**刚完成的曲目**调用 `_postHistoryFor`（携带其 trackId，
      不再依赖 `processingStateStream.completed`）；completed 仍处理最后一曲的上报
- [x] repeat 语义修复：`next()` 中 `RepeatMode.one` 短路重播当前曲；最后一曲时
      `RepeatMode.all` 回绕到 index 0，`RepeatMode.none` 自然停止；删除无条件 `% queue.length` 回绕
- [x] 手动 `next()`/`previous()` 改用 `_audioPlayer.seekToNext()`/`seekToPrevious()`
      （concat 源已加载时），不再经 `_playAtIndex → playTrack` 重建音源
- [x] `enqueue`/`enqueueNext`/`removeFromQueue`/`reorderQueue` 改为对
      `ConcatenatingAudioSource` 做 `add`/`insert`/`removeAt`/`move` 增量操作
      （`audio_handler.dart` 暴露对应方法），移除 `_refreshConcatSource` 全量重建
- [x] `resolvePlaybackUrl` 的相对路径 fallback 问题一并核实修正

### ReplayGain 实际应用（`lib/src/player/player_notifier.dart`）

- [x] 曲目开始播放/索引变化时读取 `replayGainEnabledProvider` 与 `_trackCache[id]?.replayGainDb`，
      计算 `gain = pow(10, db / 20).clamp(0.1, 2.0)`，与用户音量相乘后 `setVolume`
- [x] 用户音量与增益分离：`state.volume` 保存用户意图值，实际 `setVolume(user * gain)`；
      开关切换即时生效（`_applyVolumeWithGain`，`build()` 内 `ref.listen` 监听开关）
- [x] `replayGainDb` 为 null 时增益取 1.0（无分析数据不变化）

### Shuffle 实现（`lib/src/player/player_notifier.dart`）

- [x] `setShuffle(true)`：`_audioPlayer.setShuffleModeEnabled(true)`（concat 源原生支持），
      并调用 `_audioPlayer.shuffle()` 重洗；当前播放曲目保持不中断
- [x] `setShuffle(false)` 恢复顺序播放
- [ ] QueueSheet/队列 UI 按 `shuffleIndicesStream` 呈现实际播放顺序 —— 明确移至 v5（见下方非目标）

### EQ 真实接线（`lib/src/player/audio_handler.dart`、`pubspec.yaml`）

- [x] `InoriAudioHandler.create()` 改用
      `AudioPlayer(audioPipeline: AudioPipeline(androidAudioEffects: [AndroidEqualizer()]))`
- [x] `EqNotifier` 状态变化 → `AndroidEqualizer.parameters`：将 UI 的 10 段增益按频率
      最近邻映射到设备实际 band，`setEnabled` 同步
- [x] 删除 `applyEqualizerBands`/`resetEqualizer` 的 `(_player as dynamic).setBands` 伪实现
- [x] iOS/macOS/Windows/Linux：`_EqSection`（`settings_screen.dart`）检测平台，
      不支持时显示「当前平台暂不支持均衡器」并禁用滑块，而非静默无效

### Crossfade 如实化（`lib/src/player/audio_handler.dart`）

- [x] 移除 `_runCrossfade` 切歌后 V 形音量凹陷的伪实现
- [x] 改为「切歌淡入淡出」：position 距 duration 小于 N 秒时线性 fade-out，
      index 变化后 fade-in；音量基准取「用户音量 × ReplayGain 增益」（`targetVolume`）而非硬编码 1.0
- [x] Settings 文案由「交叉淡入淡出」改为「切歌淡入淡出」，与实际行为一致
      （真双轨 crossfade 列入后续候选，不在本阶段伪装）

### 验证

- [x] `PlayerNotifier` 单元测试：`next()` repeat-mode 决策树（T1-T3 + 中途前进用例）、
      ReplayGain 增益公式（T4-T5 + 开关关闭/极值 clamp 上下限用例）——采用纯 Dart 函数镜像
      被测逻辑的方式验证（仓库无 mocktail/mockito、无 JustAudioPlatform fake 基础设施，
      详见测试文件头注释）；`test/player_notifier_test.dart` 净增 7 个测试用例，全部通过
- [x] `flutter analyze` 0 issues（本阶段改动范围内）
- [x] `flutter test`：53 passed / 5 failed —— 失败的 5 个文件是既有生成代码问题
      （见下方"已知问题"，与 v4.6.0 记录的现象一致），已用 `git stash` A/B 对比确认与本阶段
      改动无关（stash 后 clean main 基线同为 46 passed / 5 failed，同一批文件失败）
- [ ] **人工听感验收清单**（需真机，写入 PR 描述，待用户执行）：
      Android 真机开关 EQ Bass Boost 听感差异明显；gapless 队列连播 3 曲通知栏元数据逐曲正确；
      历史页出现全部 3 条记录；ReplayGain 开关切换音量变化可感知；淡入淡出无爆音；
      shuffle 实际改变播放顺序（T6）；currentIndexStream 状态同步（T7）——这两项与人工清单
      共同构成本阶段无自动化测试seam 覆盖的收尾验证

## 非目标

- 双 `AudioPlayer` 真交叉淡化（重叠播放两轨）——列入后续候选
- presigned URL 过期前自动刷新——依赖本阶段的增量 concat 操作先落地，列入后续候选
- Web 端音频引擎（v5.2.0 处理）
- 服务端任何变更（无 OpenAPI 版本升级）

## 后续候选

- 双 player 真 crossfade（clip 重叠模型）
- presigned URL 提前 120s 刷新（`currentIndexStream` 监听 + `ConcatenatingAudioSource` removeAt/insert）
- EQ iOS 支持调研（`AVAudioUnitEQ` 需要 just_audio 之外的原生通道）
- 音量归一化模式选项（track gain / album gain 切换）

## 已知问题（非本阶段引入，记录备查）

与 v4.6.0（commit `46ff83b`）记录的现象一致：`services/mobile/lib/src/api/generated/` 生成代码
的 "language version override" 不一致问题仍未解决，导致同样的 5 个测试文件
（`artist_cache_notifier_test.dart`、`download_notifier_test.dart`、`eq_notifier_test.dart`、
`mini_player_bar_test.dart`、`track_list_tile_test.dart`）编译加载失败。本阶段用 `git stash`
做了两轮 A/B 对比：stash 本阶段改动后 clean main 基线为 46 passed / 5 failed（同一批文件失败），
恢复改动后为 53 passed / 5 failed（净增 7 个通过，即 `player_notifier_test.dart` 新增的 T1-T5
系列），证实该问题依旧与本阶段代码无关。根治仍需重新执行 `make gen:api` 并提交刷新后的生成代码，
留待 v4.8.0（测试与结构还债）阶段处理。

