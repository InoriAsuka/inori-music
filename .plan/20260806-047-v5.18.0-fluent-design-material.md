# Phase v5.18.0 — Windows Fluent Design 材质

**版本**: v5.18.0
**状态**: done（代码 + 静态验证完成；实机走查待远端 CI 构建后用户在 Windows 上确认）
**方向**: Flutter 客户端 · 对标开源/商业播放器布局改进第二阶段

## 背景

呼应参考资料"原音HQ播放器"（WinUI 3，Mica/亚克力材质）。建立在 v5.17.0 的 `DesktopAppBar` 之上（完整计划见 `/Users/inorihimea/.claude/plans/smooth-toasting-manatee.md`）。

## 目标

Windows 标题栏区域应用 Mica/亚克力半透明材质，主内容区保持不透明皮肤配色。

## 任务清单

- [x] 新增依赖 `flutter_acrylic`；核实 `Window.setEffect()`/`WindowEffect` 实际 API
- [x] `DesktopIntegration.applyTitlebarMaterial()`：Windows-only，全局启用原生材质合成，`color` 参数传入皮肤色调保证可读性
- [x] `DesktopAppBar`：材质开启时 `AppBar.backgroundColor` 设为透明，让原生模糊背景透出；其余页面不透明表面不受影响
- [x] `main.dart`：`Window.initialize()`（Windows-only）+ `ref.watch` 驱动的材质应用（该 Riverpod 版本 `listen` 无 `fireImmediately`，改用 watch）
- [x] Settings 新增"标题栏材质"选项（无/亚克力/Mica，仅 Windows 显示，复用皮肤选择器底部弹层范式）
- [x] 新增 `titlebar_material_provider.dart` + `titlebar_material_provider_test.dart`

## 验证记录

`flutter analyze --no-fatal-infos` 0 issues；`flutter test --no-pub` 153/153 通过。`flutter pub get` 引入新插件后自动更新的 `generated_plugin_registrant`/`GeneratedPluginRegistrant` 文件一并提交。实际材质效果本地无 Windows 环境无法走查，待远端 CI 构建后用户确认。
