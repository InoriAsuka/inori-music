# Phase v4.6.0 — 播放体验细节收尾

**版本**: v4.6.0
**状态**: planned
**方向**: Flutter 客户端（播放器状态/UI 缺陷修复 + 交互补全）

## 背景

规划本阶段前复核了最初提出的四个候选项在当前代码中的真实状态，发现其中两项已完整实现：
- 播放统计/「最常听」排行榜：`HistoryStatsScreen`（`lib/src/history/history_stats_screen.dart`）已完整实现汇总卡片、
  30 天活跃度柱状图、Top-5 曲目列表，并已挂载到路由 `/library/history/stats`（`history_screen.dart` 按钮跳转）。
- LRC 逐字同步精确化：v4.4.0 已实现 `LrcParser._parseWords` 解析行内 `<mm:ss.xx>` 标签，
  `full_player_screen.dart` 的 `_buildLineText` 已按字级时间戳做渐变高亮，无需进一步精确化。

复核过程中发现一处真实缺陷：`PlayerNotifier._makeMediaItem`（`lib/src/player/player_notifier.dart:338-365`）
在方法内部解析出 `albumId`（用于查 `albumTitle`），但返回的 `MediaItem.extras` 只写入了
`trackId`/`mediaObjectId` 两个 key，从未写入 `albumId`；`_stubMediaItem`（同文件 ~328 行）同样缺失。
而 `MiniPlayerBar`（`mini_player_bar.dart:42`）与 `FullPlayerScreen`（`full_player_screen.dart:124`）
都通过 `mediaItem?.extras?['albumId']` 取值传给 `artworkUrlProvider`——三处交叉确认后，说明播放器内
（MiniPlayer + FullPlayerScreen）封面图自 v3.1.0 上线以来实际上从未正确显示，始终回退到占位音符图标，
而 v3.1.0 引入的 `GET /api/v1/catalog/albums/{id}/artwork` 端点和 `artworkUrlProvider` 从未被播放器路径
真正触达。这是本阶段优先级最高的修复项。

另外两项候选复核后确认仍是真实缺口，但范围比最初设想更小：
- MiniPlayer 当前完全没有进度指示（`mini_player_bar.dart` 通篇无 `Slider`/进度条控件），
  "拖拽吸附" 需要先补一条可交互进度条，而非在已有进度条上加吸附行为。
- EQ 设置持久化已经工作正常（`EqNotifier._persist`/`_restore` 经 SharedPreferences 落盘/恢复），
  缺口只在于「自定义预设的保存与命名」——`setBand` 修改频段后只会把 `preset` 标成固定字符串 `'custom'`，
  没有保存为具名预设、切换已保存预设、删除已保存预设的能力。

## 目标

- 播放器（MiniPlayer + FullPlayerScreen）在有专辑封面时能正确显示封面图，不再静默回退占位图标
- MiniPlayer 新增可拖拽的播放进度条，支持点击/拖动跳转播放位置
- EQ 设置支持将当前频段保存为具名自定义预设，可在内置预设旁切换/重命名/删除

## 任务清单

### 播放器封面图缺陷修复（`lib/src/player/player_notifier.dart`）

- [ ] `_makeMediaItem` 的 `extras` map 补充 `'albumId': albumId.isNotEmpty ? albumId : null`
- [ ] `_stubMediaItem` 的 `extras` map 同步补充 `albumId`（从 `_trackCache[trackId]?.albumId` 取值，
      未缓存时为 null，不阻塞 stub 阶段渲染）
- [ ] 人工验证：播放一首有封面专辑的曲目，确认 MiniPlayerBar 与 FullPlayerScreen 均正确显示封面
      （而非音符占位图标）

### MiniPlayer 拖拽进度条（`lib/src/player/mini_player_bar.dart`）

- [ ] 在标题/艺术家文本下方或条形容器顶部新增紧凑进度条（`Slider` 或自绘 `LinearProgressIndicator` + 手势层）
- [ ] 拖动时调用 `playerProvider.notifier.seekTo(...)`，松手前本地维护拖拽中的临时进度值，
      避免 `positionStream` 更新与拖拽手势的进度值互相打架造成跳动
- [ ] 缓冲中（`isBuffering`）时禁用拖拽，与现有播放/暂停按钮禁用逻辑保持一致

### EQ 自定义预设保存与命名（`lib/src/audio/eq_settings.dart`、`eq_notifier.dart`、`settings_screen.dart`）

- [ ] `EqSettings` 扩展 `customPresets: Map<String, List<double>>` 字段（预设名 → 10 段增益值）
- [ ] `EqNotifier` 新增 `saveCurrentAsPreset(String name)` / `deleteCustomPreset(String name)` /
      `selectCustomPreset(String name)` 方法
- [ ] `_persist`/`_restore` 的 JSON 编解码覆盖 `customPresets`
- [ ] `_EqSection`（`settings_screen.dart:578`）新增「保存为新预设」入口（弹出命名输入对话框），
      内置预设 `SegmentedButton` 旁追加自定义预设列表（支持点击切换、滑动/长按删除）

### 验证

- [ ] `flutter analyze` 0 issues
- [ ] `flutter test` 通过；为 `EqNotifier` 新增自定义预设保存/删除/持久化的单元测试

## 非目标

- 不引入服务端 EQ 预设云同步（自定义预设仍只存本地 `SharedPreferences`，不跨设备）
- 不改动 `FullPlayerScreen` 现有进度条交互（该处已具备完整的拖拽 seek 能力，只需修 MiniPlayer 侧）
- 不新增播放统计相关 UI（`HistoryStatsScreen` 已完整实现，本阶段不重复建设）
- 不改动 LRC 解析逻辑或逐字高亮算法（v4.4.0 已完成，无需进一步精确化）
- 不做 Artist 封面（沿用 v3.1.0 既有决定，Artist 封面仍不在范围内）

## 后续候选

- MiniPlayer 长按/上滑弹出快捷操作菜单（跳到队列、加入收藏）
- EQ 自定义预设导入/导出（分享预设文件到其他设备）
- 队列拖拽排序从 MiniPlayer 直接呼出（当前仅 FullPlayerScreen 内的 QueueDrawer 支持重排）
- TrackListTile 等其他展示封面的路径做一次全量回归检查，确认是否存在同类「本地变量算出但未透传」的缺陷模式
