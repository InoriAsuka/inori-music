# Plan: v3.2.0 User Playlists

**Status**: done
**Version**: v3.2.0
**Date**: 2026-06-29

## Summary

Implemented full user personal playlist feature across Go server and Flutter client.

## Tasks

### Go Server (`services/api/`)

- [x] `internal/userplaylist/types.go` — UserPlaylist model, Repository interface, CreateRequest, UpdateRequest, sentinel errors
- [x] `internal/userplaylist/service.go` — Service with CreatePlaylist, ListPlaylists, GetPlaylist (ownership check), UpdatePlaylist, DeletePlaylist, AddTrack, RemoveTrack, SetTracks
- [x] `internal/userplaylist/memory_repository.go` — sync.RWMutex in-memory Repository (dev/test)
- [x] `internal/userplaylist/postgres/repository.go` — pgxpool Repository with DDL migration, transactional Save (upsert + track replace), Get (JOIN tracks), ListByUser (newest first), Delete
- [x] `internal/httpapi/handler.go` — Handler struct field + WithUserPlaylistService option + 9 routes under `/api/v1/me/playlists/...` + writeError cases for userplaylist errors
- [x] `cmd/server/main.go` — wire userPlaylistRepository + userPlaylistService + WithUserPlaylistService option
- [x] `internal/userplaylist/service_test.go` — 9 test cases covering all service methods

### Flutter Client (`services/mobile/`)

- [x] `lib/src/user_playlist/user_playlist_notifier.dart` — UserPlaylist model + UserPlaylistNotifier (AsyncNotifier) with load/create/delete/addTrack/removeTrack/rename/getTrackIds; userPlaylistProvider
- [x] `lib/src/favorites/favorites_screen.dart` — Library tab; added My Playlists section with create button, playlist list → push to detail; renames AppBar to "Library"
- [x] `lib/src/user_playlist/user_playlist_detail_screen.dart` — shows track IDs, Play All button, rename/delete actions in AppBar
- [x] `lib/src/shared/router.dart` — AppRoutes.myPlaylists + myPlaylistDetail + myPlaylistDetailPath(); route wired under ShellRoute
- [x] `lib/src/shared/widgets/track_list_tile.dart` — onLongPress → showModalBottomSheet with _AddToPlaylistSheet listing playlists + "New Playlist" action

## Verification

- `go build ./...` — success (services/api/)
- `go test ./internal/userplaylist/... -v` — 9 tests passed
- `flutter analyze lib/` — 0 errors (1 pre-existing unused-import warning in artists_screen.dart)
