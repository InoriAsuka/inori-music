# Phase v4.3.0 — 搜索增强（Meilisearch 替换 PostgreSQL 全文搜索）

**版本**: v4.3.0  
**状态**: planned  
**方向**: 全栈（Go 服务端新搜索后端 + Docker Compose 新服务 + Flutter 搜索 UI 升级）

## 背景

v1.x 的 `GET /api/v1/catalog/search` 使用 PostgreSQL `to_tsvector` 全文搜索，存在以下局限：
- 无拼写容错（`battover` 不能匹配 `beethoven`）
- CJK 中文分词依赖 `pg_jieba` 扩展，部署复杂
- 无 autocomplete / 搜索建议

v4.3.0 引入 Meilisearch 作为搜索后端，保留 PG 全文搜索作降级路径，响应 schema 不变，Flutter 侧仅做 UI 增强。

## 目标

- Meilisearch 替换 PG 全文搜索，支持拼写容错和 CJK 分词
- Meilisearch 不可用时自动降级到 PG，对客户端透明
- Flutter SearchScreen 新增 autocomplete + Filter Tab

## 任务清单

### 基础设施

- [ ] **Docker Compose 新增 `meilisearch` 服务**
  - `docker-compose.prod.yml`（或 `docker-compose.yml`）追加：
    ```yaml
    meilisearch:
      image: getmeili/meilisearch:latest
      ports: ["7700:7700"]
      environment:
        MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
        MEILI_ENV: production
      volumes:
        - meilisearch_data:/meili_data
    ```
  - `volumes` 段追加 `meilisearch_data:`
  - 环境变量 `MEILI_MASTER_KEY` 加入 `.env.example`
  - 文件：`docker-compose.prod.yml`（及 `.env.example` 若存在）

### Go 服务端

- [ ] **`internal/search` package — SearchService 接口**
  - `services/api/internal/search/service.go`
    ```go
    type SearchResult struct {
      Artists []catalog.Artist
      Albums  []catalog.Album
      Tracks  []catalog.Track
    }
    type Service interface {
      Search(ctx context.Context, q string, limit int) (SearchResult, error)
      IndexTrack(ctx context.Context, t catalog.Track) error
      IndexAlbum(ctx context.Context, a catalog.Album) error
      IndexArtist(ctx context.Context, a catalog.Artist) error
    }
    ```
  - 文件：`services/api/internal/search/service.go`

- [ ] **Meilisearch 实现**
  - `go get github.com/meilisearch/meilisearch-go`
  - `services/api/internal/search/meilisearch/service.go`
  - 创建三个 index（`tracks`、`albums`、`artists`），初始化时设置 `searchableAttributes` 和 `typoTolerance`
  - `Search()` 并发查三个 index，归并结果，`limit` 均分或 tracks 优先
  - Meilisearch 连接失败时 `New()` 返回 nil（调用方降级到 PG）
  - 文件：`services/api/internal/search/meilisearch/service.go`

- [ ] **PG 降级实现**
  - `services/api/internal/search/postgres/service.go`
  - 直接复用 `catalog.Service.Search()` 已有逻辑，包装为 `search.Service` 接口
  - 文件：`services/api/internal/search/postgres/service.go`

- [ ] **索引同步钩子（catalog.Service 写入后触发）**
  - `services/api/internal/catalog/service.go`：`Service` 新增可选 `searchService search.Service` 字段
  - `WithSearchService(s search.Service) *Service` 方法
  - `CreateArtist` / `UpdateArtist`、`CreateAlbum` / `UpdateAlbum`、`ImportTrack` / `UpdateTrack` 成功后 goroutine 异步推送
  - 文件：`services/api/internal/catalog/service.go`

- [ ] **HTTP handler 切换搜索后端**
  - `services/api/internal/httpapi/handler.go`
  - `Handler` 新增 `searchService search.Service` 字段
  - `search()` handler：若 `searchService != nil` 用 Meilisearch，否则调 `catalogService.Search()`
  - 结果映射到 `CatalogSearchResult` schema（现有）
  - 文件：`services/api/internal/httpapi/handler.go`

- [ ] **服务注册（`cmd/api/main.go`）**
  - 读取 `MEILI_HOST`（默认 `http://localhost:7700`）和 `MEILI_MASTER_KEY` 环境变量
  - 尝试初始化 `meilisearch.New()`，失败时 log.Warn 并使用 PG 降级
  - 文件：`services/api/cmd/api/main.go`

### OpenAPI contract

- [ ] **无 schema 变更**（`CatalogSearchResult` 结构不变）
- [ ] **版本升至 `4.3.0`**
  - 文件：`packages/api-contract/openapi/storage-admin.v1.json`

### Flutter 客户端

- [ ] **SearchScreen autocomplete**
  - 读 `services/mobile/lib/src/search/search_screen.dart`（或等效路径）
  - TextField 下方浮层：debounce 150ms，当 q.length ≥ 2 时请求 `GET /catalog/search?q=`
  - 展示前 5 条 Tracks 建议（图标 + 标题 + 艺术家）
  - 点击建议直接播放该曲目，`lyricsProvider` 预取
  - 文件：`services/mobile/lib/src/search/search_screen.dart`

- [ ] **搜索结果 Filter Tab**
  - 搜索结果页顶部 `TabBar`：All / Artists / Albums / Tracks
  - All Tab = 现有分类展示；Artists / Albums / Tracks Tab 只展示对应类型全量列表（分页）
  - 文件：`services/mobile/lib/src/search/search_screen.dart`

- [ ] **空结果状态**
  - 当结果三类均为空时，展示居中插图（`Icons.search_off`，onSurfaceVariant）+ 提示「没有找到相关内容」
  - 文件：`services/mobile/lib/src/search/search_screen.dart`

- [ ] **flutter analyze 0 errors**

### Go 服务端测试

- [ ] **`search` package 单元测试**
  - `services/api/internal/search/search_test.go`
  - Mock Meilisearch 客户端：正常搜索返回结果 / Meilisearch 不可用时降级到 PG / 空查询返回空结果
  - ≥ 3 cases
  - 文件：`services/api/internal/search/search_test.go`

## 非目标

- Meilisearch 管理 UI（index 配置通过 API key + 初始化代码完成，不需要 Web UI）
- 拼音搜索（CJK 分词依赖 Meilisearch 内置 CJK 分词器，拼音需要额外插件，留后续）
- 全量重建索引 CLI（v4.3.0 通过 catalog 写入时同步，不做批量 re-index 工具）
- Flutter 搜索历史记录

## 后续候选

- 全量 re-index CLI 工具（`cmd/reindex/main.go`）
- 拼音搜索（Meilisearch 拼音插件或服务端拼音展开）
- 搜索历史（本地 `SharedPreferences` 存最近 10 条）
- 搜索结果 Highlight（Meilisearch `highlightPreTag` / `postTag` 映射到 Flutter `RichText`）
