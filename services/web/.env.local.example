# Backend API base URL (used by Next.js rewrites in dev)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Admin token for admin-only API calls (server-side only, never NEXT_PUBLIC_)
INORI_ADMIN_TOKEN=change-me
